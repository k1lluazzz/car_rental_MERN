import React from 'react';
import { Box, Typography } from '@mui/material';

const RentalsPage = () => {
    return (
        <Box sx={{ padding: '20px' }}>
            <Typography variant="h4" sx={{ margin: '100px auto', textAlign: 'center' }}>
                Thuê Xe
            </Typography>
            {/* Add content for the rentals page here */}
        </Box>
    );
};

export default RentalsPage;
